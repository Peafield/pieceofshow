const Home = () => {
	return (
		<section className="absolute inset-0 flex items-center justify-center">
			<h1 className="font-sans text-2xl">Hello World!</h1>
		</section>
	);
};

export default Home;
